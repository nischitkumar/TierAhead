# E3 Results: Hardware Calibration Ladder

*Auto-generated by `build_calibration_report.py` / `render_results_md.py` on 2026-09-13 16:23 UTC. Do not hand-edit; re-run `run_e3.sh` (or `build_calibration_report.py`) to regenerate.*

Source data: `out/calibration_report.json`, plus whichever of `out/calib_transfer.json`, `out/calib_e2e.json`, `out/calib_nf4_fidelity.json`, and `../e1_roofline/out/calib_compute.json` exist.

## Methodology / provenance table

Experiments.md E3's own framing: *"a methodology table in the report listing every simulator parameter and whether it is measured here, measured in cited work, or vendor-specified"* — this table is that deliverable, generated directly from whatever calibration files actually exist on disk (never a fabricated placeholder).

| Parameter | Status | Detail |
|---|---|---|
| compute | `MISSING (run the GPU script to produce experiments/e1_roofline/out/calib_compute.json)` | run e1_roofline/measure_compute.py, or point --compute-calib-path at its output |
| transfer (alpha/beta) | `MISSING (run the GPU script to produce experiments/e3_calibration/out/calib_transfer.json)` | run measure_transfer.py |
| e2e offload policy ordering | `MISSING (run the GPU script to produce experiments/e3_calibration/out/calib_e2e.json)` | run e2e_offload_runner.py |
| nf4 routing fidelity (olmoe) | `MISSING (run the GPU script to produce experiments/e3_calibration/out/calib_nf4_fidelity.json)` | run nf4_fidelity.py |

> **4/4 parameter(s) still MISSING** -- this box (where this file was generated) has no CUDA GPU, so none of E3's own GPU scripts (`measure_transfer.py`, `e2e_offload_runner.py`, `nf4_fidelity.py`) could be run here. Run `run_e3.sh` on the GPU server and re-run this script (or let `run_e3.sh` do it automatically) to fill these in.

## Transfer-time model fit (t = alpha + size/beta)

Not yet available -- see `transfer (alpha/beta)` row above.

## PCIe offload-policy ordering (E3 step 3 exit criteria)

Expected (Experiments.md, translated to TPOT-latency units — see `policy_check.py`'s docstring): `next-layer-topk <= static-hot <= none`.

Not yet available -- see `e2e offload policy ordering` row above.

## NF4 routing-fidelity spot-check (OLMoE, pilot open item #2)

Not yet available -- see `nf4 routing fidelity (olmoe)` row above.

## Figures

None yet (needs the transfer calibration to exist).

## Analysis

*(placeholder -- not yet filled in. Once this section has real analysis, it will be preserved verbatim across re-runs instead of being reset to this checklist; only the data sections above ever get overwritten.)*

- [ ] Does the PCIe policy ordering (`next-layer-topk <= static-hot <= none`) hold at both swept residency levels?
- [ ] How does the measured PCIe alpha/beta compare to the CXL alpha/beta values cited from Sun et al. (ISCA'23) / Ji et al. (MICRO'24)? Are they the same order of magnitude?
- [ ] Is OLMoE's fp16-vs-nf4 routing agreement high enough to trust the project's Mixtral-NF4 trace-collection numbers by analogy?
- [ ] Is every simulator parameter E6 will need now covered, or is something still missing from this table?
